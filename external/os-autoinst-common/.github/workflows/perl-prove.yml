---
name: 'Perl tests'

on:
  pull_request:
  push:
    branches:
      - 'master'

jobs:
  perl-prove:
    runs-on: ubuntu-latest
    name: "Prove"
    container:
      image: perldocker/perl-tester:5.26
    steps:
      - uses: actions/checkout@v4
      - run: prove .
